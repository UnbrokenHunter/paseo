# Fork changelog

Entries here are for changes unique to this fork — not yet in, or never destined for,
`getpaseo/paseo` upstream. `CHANGELOG.md` mirrors upstream's copy verbatim (see
`.gitattributes` and `docs/release.md`) and is never hand-edited, so fork-only notes belong
here instead.

Not currently wired into release automation (GitHub release-notes sync, the public
`/changelog` page) — this is a plain reference file for now.
